name: hrms
description: "HRMS for Attendance Management."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  bloc: ^9.1.0
  geolocator: ^14.0.2
  hive: ^2.2.3
  dio: ^5.9.0
  hive_flutter: ^1.1.0
  jwt_decoder: ^2.0.1
  flutter_secure_storage: ^9.2.4
  firebase_core: ^4.2.0
  firebase_messaging: ^16.0.3
  get_it: ^8.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
